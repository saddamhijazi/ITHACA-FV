FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      dynamicMeshDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dynamicFvMesh      dynamicMotionSolverFvMesh;

motionSolverLibs ("libRBFMotionSolver.so");
motionSolver    RBFMotionSolver;
//movingPatches (left_in right_in);
movingPatches (left_in top_in right_in bot_in);
staticPatches (left_out top_out right_out bot_out);
coarseningRatio 15;

includeStaticPatches   true;
frozenInterpolation    true;

interpolation
{
    RBF  Gauss;
    focalPoint (0 0 0);
    innerRadius 5;//5.0
    outerRadius 10;
    polynomials true;

    W2Coeffs
    {
        radius     0.2;
    }
    TPSCoeffs
    {
        radius     0.4;
    }
    GaussCoeffs
    {
        radius     0.6;
    }
    IMQBCoeffs
    {
        radius     0.2;
    }
}
